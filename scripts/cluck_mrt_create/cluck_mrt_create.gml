// Feather disable all
/// @desc Creates a bundle of MRT surfaces. Like other GameMaker surfaces, these should be freed when you're done.
/// @returns {struct}
function cluck_mrt_create() {
    return {
        normal: -1,
        depth: -1
    };
};